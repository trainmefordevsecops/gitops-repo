apiVersion: v1
kind: Service
metadata:
  name: nginx-poc-svc
  namespace: default
spec:
  selector:
    app: nginx  # This must match the labels in your Deployment
  ports:
    - name: http-web
      protocol: TCP
      port: 80
      targetPort: 80
